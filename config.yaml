webServer:
  port: 8080

# Database connection — required. Defaults match docker-compose.yml credentials.
# databaseUrl: jdbc:postgresql://localhost:5432/ledstrip
# databaseUser: ledstrip
# databasePassword: ledstrip

# ── First-run seeding (optional) ─────────────────────────────────────────
# These fields are read once when the database is empty and inserted as the
# initial settings. They are ignored on all subsequent starts.
# Remove or comment them out after the first run.
#
# viewport:
#   width: 240
#   height: 135
#
# targetFPS: 25
# scanIntervalSeconds: 15
# backgroundImage: "/home/tlberglund/office-background.jpg"
#
# strips:
#   - id: 0
#     length: 40
#     position:
#       start: {x: 45, y: 10}
#       end: {x: 99, y: 18}
#   - id: 1
#     length: 40
#     position:
#       start: {x: 105, y: 19}
#       end: {x: 139, y: 22}
